---
import Layout from "../components/Layout.astro";
import Gallery from "../components/Gallery.astro";
import { site, about, projects, soloExhibitions } from "../data/content.he";
---
<Layout site={site} projects={projects} lang="he" aboutLabel="אודות וצור קשר" projectsLabel="מבחר סדרות" soloExhibitionsLabel="תערוכות יחיד" switchHref="/" switchLabel="English">
  <div id="top" class="hero">
    <img src={site.heroImage} alt={site.name} />
    <div class="hero-inner">
      <div class="hero-text">
        <h1>{site.name}</h1>
        <p>{site.tagline}</p>
      </div>
    </div>
  </div>

  {projects.map((project) => <Gallery project={project} />)}

  <div id="solo-exhibitions">
    {soloExhibitions.map((exhibition) => <Gallery project={exhibition} />)}
  </div>

  <section id="about">
    <h2 class="section-title">אודות<br /><span class="section-title-sub">צור קשר</span></h2>
    <div class="about-grid">
      <img src={about.photo} alt={`${site.name} - תמונת פורטרט`} />
      <div class="bio">
        {about.bio.map((p) => <p>{p}</p>)}
        <p style="margin-top:20px;">
          אימייל: <a href={`mailto:${site.email}`} style="color:var(--accent);">{site.email}</a>
        </p>
      </div>
    </div>
  </section>
</Layout>
